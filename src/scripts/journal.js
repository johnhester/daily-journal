// journal entry object

const journalEntry = {
    journalDate: 2020-05-29 ,
    journalConcepts: "booty",
    journalEntry: "Today......",
    mood: "😀"
}

// entry storage array
const entries =[

];

// entry retreival function
const getEntry = () => {

}

// entry storage function

const storeEntry = (entry) => {
    entries.push(entry);
}